---
  - hosts: all
    tasks:
    gather_facts: true
    sudo: yes
    roles:
      - serviced
#      - repos
#      - filesystems
#      - services
#      - docker-config
      - containers
      - complete
