---
  - hosts: all
    tasks:
    gather_facts: true
    sudo: yes
    roles:
# - checks
# - network
      - network
      - logs
      - selinux
      - filesystems
      - repos
      - serviced
      - services
      - docker-config
      - containers
      - complete
