---
  - hosts: all
    tasks:
    gather_facts: true
    sudo: yes
    roles:
# - checks
# - network
      - firewall
      - logs
      - selinux
      - filesystems
      - repos
      - docker
      - serviced
      - services
      - containers
      - complete
